<Page
    x:Class="MainUI.MissionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MainUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>

        <!-- Icon Button -->
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Background" Value="#FFEFEFEF"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Card Container Style -->
        <Style x:Key="MissionCardContainerStyle" TargetType="GridViewItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

    </Page.Resources>

    <Grid>
        <!-- Background -->
        <Image Source="Assets/HokageMonument.jpg"
               Stretch="UniformToFill"
               Opacity="0.3"
               VerticalAlignment="Stretch"
               HorizontalAlignment="Stretch"/>

        <Grid Background="#CCF0F8FF">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <StackPanel Background="#CCBDE6F1" Padding="20" Spacing="15" Grid.Column="0">
                <TextBlock Text="☁ Menu" FontSize="24" FontWeight="Bold" Foreground="#1A3C56" Margin="0,10"/>
                <Button Content="🏠 Home" Click="GoHome_Click" Style="{StaticResource ModernButtonStyle}" Height="50"/>
                <Button Content="🧍 Manage Shinobi" Click="ManageShinobi_Click" Style="{StaticResource ModernButtonStyle}" Height="50"/>
                <Button Content="📜 Manage Missions" IsEnabled="False" Style="{StaticResource ModernButtonStyle}" Height="50" Click="Button_Click"/>
            </StackPanel>

            <!-- Main Content -->
            <Grid Grid.Column="1" Padding="30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <TextBlock Text="📜 Manage Missions"
                           FontSize="32"
                           FontWeight="Bold"
                           Foreground="#1A3C56"
                           Margin="0,0,0,20"/>

                <!-- Search + Add -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Grid.Row="1" Margin="0,0,0,20">
                    <TextBox x:Name="SearchBox" Width="300" PlaceholderText="Search Missions..." Margin="0,0,20,0"/>
                    <Button Content="➕ Add Mission"  Background="#5885AF" Foreground="White" Padding="12,6"/>
                </StackPanel>

                <!-- Mission Cards -->
                <GridView x:Name="MissionList"
                          Grid.Row="2"
                          IsItemClickEnabled="False"
                          SelectionMode="None"
                          Padding="0"
                          ItemContainerStyle="{StaticResource MissionCardContainerStyle}">
                    <GridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsWrapGrid Orientation="Vertical" MaximumRowsOrColumns="2"/>
                        </ItemsPanelTemplate>
                    </GridView.ItemsPanel>

                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="WhiteSmoke"
                                    CornerRadius="10"
                                    Padding="15"
                                    Width="300"
                                    Margin="10"
                                    BorderBrush="#FFE0E0E0"
                                    BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold" Foreground="#333"/>
                                    <TextBlock Text="Rank: {Binding Rank}" Foreground="#666"/>
                                    <TextBlock Text="Assigned: {Binding AssignedShinobi}" Margin="0,4,0,0"/>
                                    <TextBlock Text="Status: {Binding Status}"/>
                                    <TextBlock Text="Due: {Binding DueDate, StringFormat='{}{0:MMM dd, yyyy}'}"/>

                                    <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,10,0,0">
                                        <Button  Style="{StaticResource IconButtonStyle}">
                                            <StackPanel Orientation="Horizontal" Spacing="6">
                                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE70F;" FontSize="16"/>
                                                <TextBlock Text="Edit"/>
                                            </StackPanel>
                                        </Button>
                                        <Button  Background="DarkRed" Foreground="White" Style="{StaticResource IconButtonStyle}">
                                            <StackPanel Orientation="Horizontal" Spacing="6">
                                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE74D;" FontSize="16"/>
                                                <TextBlock Text="Delete"/>
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </Grid>
        </Grid>
    </Grid>
</Page>
